/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { FishSearchComponent } from './fish-search.component';

describe('FishSearchComponent', () => {
  let component: FishSearchComponent;
  let fixture: ComponentFixture<FishSearchComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [FishSearchComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(FishSearchComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
}); */
